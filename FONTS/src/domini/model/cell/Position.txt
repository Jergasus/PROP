CLASS: Position
Package: model.cell
Layer: Model

Purpose:
  Immutable (row, col) coordinate pair. Implemented as a Java record.
  Used throughout the model to identify cell locations without exposing
  raw int pairs. Implements Serializable for board persistence.

Fields:
  - row(): the row index
  - col(): the column index
