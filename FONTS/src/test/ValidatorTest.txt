CLASS: ValidatorTest
Package: test
Layer: Test

Purpose:
  JUnit 4 tests for model.algorithms.Validator.
  Covers isValidSolution() and isPartiallyValid() on square (4-way and 8-way) boards.

Tests (14):
  validSolution_correct2x2             - 2x2 board with a correct consecutive path
  validSolution_emptyBoardFails        - no values placed; must be rejected
  validSolution_duplicateValueFails    - duplicate cell value; must be rejected
  validSolution_brokenPath4way         - consecutive pair not adjacent in 4-way
  validSolution_correct3x3Snake        - snake-path 3x3; must be accepted
  validSolution_broken3x3Fails         - 3x3 with a gap in the path
  validSolution_8way_orthogonalOk      - orthogonal path valid in 8-way mode
  validSolution_8way_diagonalAllowed   - diagonal step valid in 8-way mode
  validSolution_boardWithVoids         - path jumps over void cells; must be rejected
  partiallyValid_emptyBoardIsOk        - no numbers yet; no violation possible
  partiallyValid_noConflict            - single clue placed; nothing to check
  partiallyValid_conflictDetected      - consecutive clues in non-adjacent cells
  partiallyValid_gapAllowed            - non-consecutive clues in non-adjacent cells
  partiallyValid_conflict8way_diagonalOk - diagonal consecutive pair fine in 8-way
