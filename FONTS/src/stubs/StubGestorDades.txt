CLASS: StubGestorDades
Package: stubs
Layer: Persistence (stub)

Purpose:
  Placeholder for the data persistence layer (GestorDades).
  Delivery 1 has no real file I/O for rankings or user data.
  All methods return empty/false. Will be replaced by the real
  GestorDades implementation in delivery 2.

Public methods:
  - loadHidato(path): simulates loading a hidato file; returns empty array
  - saveHidato(path, lines): simulates saving; always returns false
  - loadRanking(): simulates loading a ranking; returns empty array
  - saveRanking(lines): simulates saving a ranking; always returns false
